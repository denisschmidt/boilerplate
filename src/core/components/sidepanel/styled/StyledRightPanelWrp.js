import styled from 'styled-components';

const StyledRightPanelWrp = styled.div`
  min-width: 361px;
  width: 361px;
  position: relative;
  border-left: 1px solid ${({ theme }) => theme.colors.border};
`;

export default StyledRightPanelWrp;
