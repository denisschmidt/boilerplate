import styled from 'styled-components';
import Row from '../../../components/row/Row';

const StyledModalFilters = styled(Row)`
  padding-left: 32px;
  padding-right: 32px;
  padding-bottom: 24px;
`;

export default StyledModalFilters;
