import styled from 'styled-components';

const StyledModalBlock = styled.header`
  position: relative;
  display: flex;
  justify-content: space-between;
  align-items: end;
`;

export default StyledModalBlock;
