import styled from 'styled-components';
import Row from '../../../components/row/Row';

const StyledModalFooter = styled(Row)`
  justify-content: space-between;
  padding-left: 32px;
  padding-right: 32px;
  padding-top: 24px;
`;

export default StyledModalFooter;
