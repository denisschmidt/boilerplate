import styled from 'styled-components';

const StyledNotifyTitle = styled.div`
  height: 24px;
  font-size: 18px;
  line-height: 1.33;
  letter-spacing: 0.5px;
  color: ${({ theme }) => theme.colors.blackThree};
`;

export default StyledNotifyTitle;
