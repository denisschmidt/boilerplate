import styled from 'styled-components';

const StyledNotifyText = styled.div`
  font-size: 15px;
  font-stretch: normal;
  line-height: 1.33;
  color: ${({ theme }) => theme.colors.textAdditional2};
  margin-top: 2px;
`;

export default StyledNotifyText;
