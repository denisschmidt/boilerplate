import styled from 'styled-components';

const StyledNotifyMessage = styled.div`
  display: flex;
  align-items: flex-start;
  line-height: 20px;
  justify-content: space-between;
`;

export default StyledNotifyMessage;
